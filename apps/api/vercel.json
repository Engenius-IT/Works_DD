{
  "version": 2,
  "framework": "nestjs",
  "buildCommand": "npx prisma generate --schema=./prisma/schema.prisma && tsc -p tsconfig.json",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
